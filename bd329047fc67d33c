/* Light DOM CSS for <ui-tooltip> */

ui-tooltip {
	display: inline-block;
	position: relative;
}

/* Trigger */
ui-tooltip > :not(.tooltip) {
	cursor: pointer;
	anchor-name: --trigger;
}

ui-tooltip > .tooltip {
	margin: 0;
	position: absolute;
	z-index: 2;

	/* Anchor positioning as PE */
	position-anchor: --trigger;
	position-area: bottom span-all;
	/* Currently not working: :( */
	position-try: normal flip-block flip-inline;

	/* Fallback */
	inset: auto;
	inset-block-start: 100%;
	min-width: 15em;
	text-align: start;
}

ui-tooltip.top > .tooltip {
	inset-block-start: auto;
	inset-block-end: 100%;
	position-area: top span-all;
}

ui-tooltip:not(:hover, :focus-within) > .tooltip {
	display: none;
}
